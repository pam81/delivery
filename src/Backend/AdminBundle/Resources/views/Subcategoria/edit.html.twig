{% extends '::backend.html.twig' %}

{% block container -%}
    <h3>Editar Subcategoria</h3>
    
    
    
    
    <div class="well">
    <div id="myTabContent" class="tab-content">
    <div class="tab-pane active in" id="home">
    <form class="form-horizontal" id="tab" method="post" novalidate  {{ form_enctype(form) }} action="{{ path('subcategoria_update', { 'id': entity.id }) }}" >
    <input type="hidden" name="_method" value="PUT" />
     {{ form_errors(form) }}
     
     
   
    
     {% include 'BackendAdminBundle:Subcategoria:_subcategoria.html.twig' %} 
        
    {{ form_rest(form) }}

    <div class="form-group">
    <!-- Button -->
    <div class="text-center">
    <button type="submit" class="btn btn-primary"><i class="fa fa-check"></i>Actualizar</button>
    <button type="button" class="btn btn-inverse" data-url="{{ path('subcategoria') }}" ><i class="fa fa-chevron-left"></i> Volver</button>
    {% if is_granted("ROLE_DELSUBCATEGORIA") %}
  
    <a href="#myModal" class="btn btn-danger confirm-delete" role="button" data-toggle="modal" data-id="{{ entity.id }}" ><i class="fa fa-trashe"></i>Borrar</a>
    
    {% endif %}
    
    </div>
    
    </div>
    
    </form>
    </div>
    
    </div>
     </div>
    
    <div class="modal small hide fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
     <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
      <h3 id="myModalLabel">Confirmar Borrado</h3>
     </div>
     <div class="modal-body">
       <p class="error-text">Esta seguro que desea borrar la subcategoria?</p>
     </div>
     <div class="modal-footer">
      <button class="btn" data-dismiss="modal" aria-hidden="true">Cancelar</button>
      <button class="btn btn-danger " data-id="0"  data-url="{{ path('subcategoria_delete', { 'id': 'id' }) }}" >Borrar</button>
     </div>
    </div>

<form action="" id="delete-form" method="post" {{ form_enctype(delete_form) }}>
    {{ form_widget(delete_form) }}
    <input type="hidden" value="DELETE" name="_method">
    
    </form>

    
{% endblock %}
{% block javascripts %}
  {{ parent() }}

{% javascripts '@BackendAdminBundle/Resources/public/js/validate_subcategoria.js' filter='?uglifyjs2'%}
    <script type="text/javascript" src="{{ asset_url }}"></script>
{% endjavascripts %}
{% endblock %}
